# 公開分享與強制更新

## 分享自己的 OC 介紹

發布 GitHub Pages 後，將網站網址分享給別人即可：

```text
https://kanamememe.github.io/About_my_OC/
```

網站右上角的 **分享此頁** 會分享目前正在看的頁面，包括特定世界、角色或 IF 線。支援系統分享選單的裝置會直接開啟分享介面；其他瀏覽器會複製網址。

## 訪客能不能修改內容？

不能。公開網站是唯讀展示頁，沒有編輯、儲存或刪除功能。訪客在自己瀏覽器中暫時修改畫面，也不會改變 GitHub 上的網站內容。

真正能修改網站檔案的人，只有擁有此 GitHub repository 寫入權限的帳號。因此不要把不需要共同編輯的人加入 Collaborators。

> Repository 目前是公開的，因此其他人可以查看或複製程式碼，但無法修改你的原始 repository。網頁內容也無法從技術上完全阻止截圖、複製或另存。

## 強制更新

網站右上角的 **強制更新** 會：

1. 產生新的時間戳版本。
2. 重新載入 `style.css`、`data.js`、`app.js`、`controls.css` 與 `controls.js`。
3. 繞過瀏覽器先前保存的舊版檔案。
4. 更新完成後自動移除網址中的更新參數。

當你在 GitHub 修改 OC 資料並發布後，訪客若仍看到舊內容，可以按下此按鈕。

GitHub Pages 本身部署新版本仍可能需要短暫時間；在部署尚未完成前按下更新，仍會載入上一個已發布版本。

## 離線版

下載到裝置的離線版也能按 **強制更新**，但它只會重新讀取裝置裡現有的檔案。離線檔案不會自動從 GitHub 下載新版，也不能產生可供他人開啟的公開連結。
